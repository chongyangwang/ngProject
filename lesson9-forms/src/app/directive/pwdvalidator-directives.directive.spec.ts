import { PwdvalidatorDirectivesDirective } from './pwdvalidator-directives.directive';

describe('PwdvalidatorDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new PwdvalidatorDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
