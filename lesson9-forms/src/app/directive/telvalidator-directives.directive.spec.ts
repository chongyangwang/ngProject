import { TelvalidatorDirectivesDirective } from './telvalidator-directives.directive';

describe('TelvalidatorDirectivesDirective', () => {
  it('should create an instance', () => {
    const directive = new TelvalidatorDirectivesDirective();
    expect(directive).toBeTruthy();
  });
});
